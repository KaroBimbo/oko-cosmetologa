# План сдачи ДЗ

## 1. Подготовить ключи

1. Получить `APIFY_TOKEN` в Apify.
2. Получить `OPENROUTER_API_KEY` в OpenRouter.
3. Выбрать модель и записать ее в `OPENROUTER_MODEL`.

Минимальный вариант:

```bash
OPENROUTER_MODEL=openrouter/owl-alpha
```

Если модель недоступна или упирается в лимит, замените ее на любую доступную дешевую модель в OpenRouter.

## 2. Проверить локально

```bash
npm install
npm run dev -- -p 3006
```

Открыть `http://localhost:3006` и проверить демо-кейс:

- город: `Санкт-Петербург`
- услуга: `контурная пластика`
- лимит: `5`

Важно: если ключи не добавлены, сайт все равно открывается, но реальный сбор данных не запустится.

## 3. Опубликовать на Vercel

1. Импортировать GitHub-репозиторий в Vercel.
2. Добавить Environment Variables:
   - `APIFY_TOKEN`
   - `OPENROUTER_API_KEY`
   - `OPENROUTER_MODEL`
   - при необходимости actor overrides из `.env.example`
3. Нажать Deploy.
4. Открыть опубликованную ссылку и сделать один тестовый анализ.

## 4. Отправить в школу

1. В поле ответа вставить ссылку на Vercel-сайт.
2. Через кнопку выбора файла загрузить любой файл проекта до 200 МБ, например `README.md`.

## Что уже сделано в проекте

- Есть веб-интерфейс для ввода города, услуги, лимита, Instagram-профилей и источников.
- Есть серверный route `POST /api/analyze`, чтобы API-ключи не попадали в браузер.
- Есть сбор через Apify, анализ через OpenRouter и вывод результата на сайте.
- Есть обработка ошибок, демо-данные и предупреждения о неполных данных.
